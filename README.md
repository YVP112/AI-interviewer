🚀 Virtual Technical Interviewer — AI-интервьюер нового поколения

Virtual Technical Interviewer (VI) — это система, которая полностью автоматизирует техническое собеседование инженеров:
теория → live-coding → анализ → итоговый отчёт.

В основе — LLM (Qwen 30B Coder), адаптивная логика, память состояний, анти-читинг и собственная песочница для выполнения кода.

Работает как настоящий senior-инженер уровня FAANG.

⚙️ Технологический стек
Компонент	Технология
LLM	Qwen 30B Coder (через API)
Backend	FastAPI
Memory Engine	Контроллер стадий интервью
Sandbox	Isolated Python subprocess
Frontend	React + Monaco Editor
Контейнеризация	Docker + docker-compose
🎯 Что умеет наш интервьюер
✅ 1. Адаптивное техническое собеседование

ИИ оценивает ответы и меняет сложность вопросов:

LEVEL 1 → простые вопросы

LEVEL 2 → практические концепции

LEVEL 3 → архитектура

LEVEL 4 → senior+

Плохие ответы понижают уровень, хорошие — повышают.
3 неудачных ответа подряд → кандидат не допускается к кодингу.

✅ 2. Полный live-coding с тестами

После теории кандидат переходит к кодингу:

даём задачу из фиксированного task-банка

кандидат пишет код в Monaco Editor

sandbox запускает тесты

модель анализирует ошибки

максимум 2 подсказки

успешный тест → новая задача, более сложная

провал → итоговый отчёт

LLM не имеет права придумывать новые задачи.

📚 Task Bank (строго фиксированный)

L1–L4, используем только эти задачи — это правило enforce-ится в промптах.

reverse_string
sum_array
is_palindrome
count_vowels
max_of_three

validate_parentheses
two_sum
remove_duplicates
rotate_array
longest_common_prefix

flatten_list
max_subarray
top_k
merge_intervals

lru_cache
trie

🧠 Memory Engine (сердце системы)

Хранит:

историю диалога

текущую фазу интервью:
theory → coding → feedback → final

уровень задач

количество подсказок

текущую задачу

результаты выполнения

Memory управляет переходами, чтобы LLM всегда соблюдал сценарий.

🧪 Sandbox для выполнения кода

Безопасная изоляция:

subprocess с ограничениями

автогенерация временных тестов

перехват stdout/stderr

безопасная очистка окружения

невозможность импорта системных модулей

Возвращает:

статус success/fail

список результатов тестов

ошибки исполнения

🧩 Архитектура системы
Frontend (React, Monaco Editor)
     |
     |  /chat
     |  /code/run
     v
Backend (FastAPI)
     ├── qwen_client.py       – логика LLM, стадии, задачи
     ├── memory.py            – состояние интервью
     ├── tasks.py             – база задач L1–L4
     ├── sandbox.py           – выполнение Python-кода
     └── prompts/             – FAANG системный промпт
     
LLM API (Qwen Coder 30B)

🖥 Запуск проекта
🚢 Docker (рекомендуется)
docker compose up --build


Порт фронтенда:
http://localhost:5173

Порт бэкенда (Swagger):
http://localhost:8000/docs

💡 Сценарий работы

Кандидат начинает интервью — 5 теоретических вопросов.

Если проход успешен → интервьюер выдаёт coding-задачу.

Sandbox запускает код → LLM анализирует результат.

Если success=True → следующая задача (сложнее).

Если success=False → подсказка (max 2).

После завершения → полный отчёт:

теория (%),

практика (%),

сильные стороны,

зоны роста,

итоговый вердикт.

📊 Почему это важно

интервьюеры тратят 50–70% времени на рутинные этапы

субъективность → снижает качество найма

онлайн-собеседования требуют анти-читинга

масштабируемость ограничена количеством людей

компании хотят единый стандарт оценки

Наш интервьюер решает все эти проблемы.

🏁 Статус проекта

✔ архитектура реализована

✔ адаптивная теория

✔ live-coding с sandbox

✔ защита от читинга

✔ красивый UI

✔ финальные отчёты

Готово к демонстрации и масштабированию.

📎 Лицензия

MIT
